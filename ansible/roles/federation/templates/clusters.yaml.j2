apiVersion: federation/v1beta1
kind: Cluster
metadata:
  name: {{cluster_name}}
spec:
  serverAddressByClientCIDRs:
    - clientCIDR: "0.0.0.0/0"
      serverAddress: "https://{{ master_ip }}:8443"
  secretRef:
    name: {{cluster_name}}
